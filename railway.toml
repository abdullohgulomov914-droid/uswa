[build]
builder = "nixpacks"

[deploy]
startCommand = "cd Untitled/backend && npm run db:init && npm start"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
